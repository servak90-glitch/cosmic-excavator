# 📖 РУКОВОДСТВО ОПЕРАТОРА: OMEGA TERMINAL (DevTools)

> **Версия**: 1.0.0  
> **Дата**: 2026-01-19  
> **Проект**: Cosmic Excavator: Void-Piercer

---

## 🔓 КАК ОТКРЫТЬ OMEGA TERMINAL

### Способ активации:

1. **Откройте настройки**: Нажмите на кнопку ⚙️ (Settings) в правом верхнем углу экрана
2. **Секретная активация**: Кликните **7 раз** подряд на заголовок "НАСТРОЙКИ" (каждый клик должен быть не позже чем через 1 секунду после предыдущего)
3. **Введите пароль**: Появится поле ввода ACCESS CODE
4. **Введите код**: `VOID_ADMIN`
5. **Подтвердите**: Нажмите ENTER

### Условия доступа:

- ✅ DevTools доступен **только после активации** секретным паролем
- ✅ После активации кнопка `[OPEN DEBUG CONSOLE]` появится в настройках постоянно
- ✅ DevTools **сохраняет состояние** (debugUnlocked) между сессиями игры

### Открытие консоли после активации:

1. **Из настроек**: ⚙️ Settings → `[OPEN DEBUG CONSOLE]`
2. **Перемещение окна**: Зажмите заголовок "OMEGA TERMINAL [ROOT]" и перетащите окно в нужное место (поддержка мыши и тачскрина)
3. **Закрытие**: Нажмите кнопку `[X]` в правом верхнем углу терминала

---

## 📋 ВКЛАДКИ OMEGA TERMINAL

DevTools разделён на 5 вкладок:

### 1️⃣ **RES (Resources)**
**Управление ресурсами, опытом и артефактами**

#### Быстрые действия:
- `+1M COM` — Добавить 1 миллион обычных ресурсов (stone, copper, iron, etc.)
- `+1K RARE` — Добавить 1000 редких ресурсов (uranium, nanoSwarm, ancientTech, gems)
- `+1M XP` — Добавить 1 миллион опыта
- `+1 LVL` — Добавить 1 уровень игроку
- `ID ALL ARTIF.` — Идентифицировать все артефакты в инвентаре
- `RESET WALLET` — Сбросить все ресурсы до 0

#### Спавнер артефактов:
- **Dropdown список**: Выберите нужный артефакт из полного списка
- **Кнопка ADD**: Добавляет выбранный артефакт в инвентарь
- Показывает название и редкость артефакта

---

### 2️⃣ **STATE (Game State Toggles)**
**Переключатели состояния игры и читы**

#### Переключатели (Toggle):
- `GOD MODE` — Бессмертие (игнорирование урона, перегрева, etc.)
- `INF. COOLANT` — Бесконечное охлаждение (heat не растёт)
- `OVERDRIVE (x100)` — Скорость игры x100 (осторожно!)
- `INFINITE FUEL` — Бесконечное топливо (travel не требует coal/oil/gas)
- `INFINITE ENERGY` — Бесконечная энергия
- `ZERO WEIGHT` — Нулевой вес груза (currentCargoWeight = 0)

#### Быстрые действия:
- `UNLOCK ALL` — Разблокировать всё: Market, Caravans, все лицензии и пермиты
- `MAX SKILLS` — Прокачать все навыки до максимума
- `MAX DRONES` — Максимальное количество дронов
- `MAX TECH` — Разблокировать все технологии
- `MAX REP` — Максимальная репутация у всех фракций
- `INSTANT HEAL` — Моментально восстановить здоровье/целостность бура
- `FINISH QUESTS` — Завершить все активные квесты
- `FINISH EXPEDITIONS` — Мгновенно завершить все экспедиции/путешествия

#### Фракции и события:
- `+CORP` — Добавить 100 репутации у Корпорации
- `+SCI` — Добавить 100 репутации у Учёных
- `+REB` — Добавить 100 репутации у Повстанцев
- `CLEAR EVT` — Очистить очередь событий
- `CLEAR EFF` — Очистить все активные эффекты (баффы/дебаффы)
- `TRIGGER SIDE TUNNEL` — Принудительно вызвать событие "Боковой туннель"

---

### 3️⃣ **NAV (Navigation)**
**Навигация по глубине и биомам**

#### Быстрые прыжки:
- `+10k M` — Телепорт на +10,000 метров глубже
- `+50k M` — Телепорт на +50,000 метров глубже
- `SKIP BIOME` — Перейти к следующему биому

#### Управление боссами:
- `SPAWN BOSS` — Принудительно заспавнить босса текущего биома
- `KILL BOSS` — Мгновенно убить текущего босса

#### Ручной прыжок:
- **Поле METERS**: Введите точную глубину (например, `25000`)
- **Кнопка OK**: Телепортирует на указанную глубину

---

### 4️⃣ **EVENT (Event Trigger)**
**Принудительный вызов событий**

#### Список событий:
- Показывает **все события** из eventRegistry
- Формат: `event_id - Название события`
- **Клик** на событие = мгновенный триггер

#### Управление:
- `CLEAR QUEUE` — Очистить очередь событий

> **Примечание**: События вызываются мгновенно, игнорируя probabilityModel и triggers

---

### 5️⃣ **GLOBAL (Global Map Systems)**
**Управление системами глобальной карты**

#### ⛽ Топливо (FUEL):
- `+1K COAL` — Добавить 1000 угля
- `+500 OIL` — Добавить 500 нефти
- `+250 GAS` — Добавить 250 газа

#### 🌍 Телепортация (TELEPORT):
- **Кнопки регионов**: Мгновенная телепортация в любой регион
  - Rust Valley
  - Crystal Wastes
  - Iron Gates
  - Magma Core
  - Void Chasm
- **Подсветка**: Текущий регион выделен зелёным

#### 🎫 Лицензии (LICENSES):
- `UNLOCK ALL LICENSES` — Разблокировать все зональные лицензии (Green, Yellow, Red)
- `GREEN` — Разблокировать Green Zone License
- `YELLOW` — Разблокировать Yellow Zone License
- `RED` — Разблокировать Red Zone License
- `UNLOCK ALL PERMITS` — Разблокировать все региональные пермиты (постоянные)

#### 🏗️ Базы (CREATE BASE / RAID):
- `OUTPOST` — Создать аванпост в текущем регионе
- `CAMP` — Создать лагерь в текущем регионе
- `STATION` — Создать станцию в текущем регионе
- `INSTANT CONSTRUCTION` — Мгновенно завершить строительство всех баз
- `FORCE BASE RAID` — Принудительно вызвать рейд на случайную базу

#### 🚛 Караваны (CARAVAN):
- `UNLOCK 1★ SHUTTLE` — Разблокировать 1-звёздочный шаттл (базовая логистика)

#### 📦 Груз (CARGO):
- `EMPTY` — Установить вес груза = 0 кг
- `5000KG` — Установить вес груза = 5000 кг

---

## ⚙️ ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Состояние хранения:
- `debugUnlocked` сохраняется в localStorage проекта
- Поле в `GameState` интерфейсе: `debugUnlocked: boolean`
- Управление: `setDebugUnlocked(unlocked: boolean)`

### Условия рендера:
```typescript
if (!store.isDebugUIOpen || !store.debugUnlocked) return null;
```

### Поддержка платформ:
- ✅ **Desktop**: Полная поддержка мыши (drag-and-drop окна)
- ✅ **Mobile**: Полная поддержка тачскрина (touch drag)
- ✅ **Responsive**: Адаптивные размеры (w-80 на мобильном, w-96 на десктопе)

---

## 🚨 ПРЕДУПРЕЖДЕНИЯ

### ⚠️ Осторожно с OVERDRIVE!
- Скорость игры x100 может вызвать:
  - Переполнение ресурсов
  - Сбой анимаций
  - Проблемы с рендерингом
- **Рекомендация**: Используйте только для быстрого фарма

### ⚠️ INFINITE режимы:
- `INFINITE FUEL`, `INFINITE ENERGY`, `ZERO WEIGHT` — Изменяют геймплей кардинально
- Могут нарушить баланс игры
- **Для отладки**: Идеально для тестирования новых систем

### ⚠️ Сохранения:
- DevTools **НЕ отключает autosave**
- Все изменения (читы, ресурсы, etc.) **сохраняются**
- Для "чистого" старта используйте `RESET WALLET` или сброс прогресса через Settings

---

## 📝 ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ

### Пример 1: Быстрое тестирование Global Map
```
1. STATE → UNLOCK ALL
2. GLOBAL → UNLOCK ALL LICENSES
3. GLOBAL → UNLOCK ALL PERMITS
4. GLOBAL → +1K COAL (x5 раз)
5. GLOBAL → CREATE BASE → STATION (в каждом регионе)
6. STATE → INFINITE FUEL (включить)
7. Тестирование системы путешествий
```

### Пример 2: Тестирование событий
```
1. EVENT → Выбрать "SIDE_TUNNEL_DISCOVERY"
2. Кликнуть на событие
3. Проверить UI реакцию
4. CLEAR QUEUE
```

### Пример 3: Быстрая прокачка персонажа
```
1. RES → +1M XP (10 раз)
2. STATE → MAX SKILLS
3. STATE → MAX TECH
4. STATE → MAX DRONES
5. RES → ID ALL ARTIF.
```

---

## 🔍 FAQ

### Q: Пароль не работает?
**A**: Проверьте раскладку клавиатуры. Пароль: `VOID_ADMIN` (английские заглавные буквы).

### Q: DevTools не открывается после активации?
**A**: Откройте Settings → `[OPEN DEBUG CONSOLE]` (кнопка появляется только после ввода пароля).

### Q: Как отключить читы?
**A**: Переключите нужные toggle в STATE обратно на OFF.

### Q: DevTools пропал после перезагрузки?
**A**: `debugUnlocked` сохраняется. Просто откройте его через Settings → `[OPEN DEBUG CONSOLE]`.

### Q: Можно ли сбросить DevTools доступ?
**A**: Да. Settings → Reset Progress ИЛИ вручную очистите localStorage.

---

## 📚 СВЯЗАННЫЕ ФАЙЛЫ

- **Компонент**: `components/DevTools.tsx`
- **Slice**: `store/slices/adminSlice.ts`
- **Settings**: `store/slices/settingsSlice.ts`
- **UI**: `components/SettingsModal.tsx`

---

**🎮 Приятной отладки, оператор!**
